<!doctype html>
<html lang="fr">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

       <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">

    <link href="/css/style.css" rel="stylesheet">
    <link href="/css/floating-labels.css" rel="stylesheet">

    <title>O'quiz</title>
</head>

<body>
        <header
            class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-primary text-dark border-bottom shadow-sm">
            <h1 class="h5 my-0 mr-md-auto font-weight-normal">O'quizz</h1>
            <nav class="my-2 my-md-0 mr-md-3">
                <a class="p-2 text-dark" href="/">Accueil</a>
                <a class="p-2 text-dark" href="/tags">Sujets</a>
                <% if(user) { %>
                    <a class="p-2 text-dark" href="/profile">Mon compte</a>
                    <a class="p-2 text-dark" href="/disconnect">Déconnexion</a>
                    <% if(user.role == "admin") { %>
                        <a class="p-2 text-dark" href="/admin">Admin</a>
                    <% } %>
                <% } else {%>
                    <a class="p-2 text-dark" href="/signup">S'inscrire</a>
                    <a class="p-2 text-dark" href="/login">Se connecter</a>
                <% } %>
            </nav>
        </header>
        <main class="container">
